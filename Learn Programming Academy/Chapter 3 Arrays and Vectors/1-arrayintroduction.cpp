#include <iostream>

int main ()
{
    int test_scores [5] = {100, 95, 82, 51, 129};

    int high_score_per_level [10]= {3, 5};    // first 2 values are 3 and 5, while the rest will remain as 0;

    const double days_in_year {365};    // int all turns to 0
    double hi_temperatures = {0};

    int another_array [] = {1, 2, 3, 4 ,5};

    return 0;
}